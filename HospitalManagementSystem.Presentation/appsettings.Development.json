{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "HMSConnection": "Server=(localdb)\\MSSQLLocalDB;Database=HospitalDB;Trusted_Connection=True;TrustServerCertificate=True"
  }
  // TokenKey should be stored in User Secrets for development
  // Run: dotnet user-secrets init
  // Then: dotnet user-secrets set "TokenKey" "your-secret-key-here"
  // And: dotnet user-secrets set "TokenKey:Issuer" "HospitalManagementSystem"
  // And: dotnet user-secrets set "TokenKey:Audience" "HospitalManagementSystemClient"

}
